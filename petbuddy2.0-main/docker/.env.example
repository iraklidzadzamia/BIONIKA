# =============================================================================
# Docker-Specific Environment Configuration
# =============================================================================
# This file is for Docker Compose only (MongoDB credentials)
#
# ⚠️  IMPORTANT: For application configuration, use the individual package .env files:
#    - packages/backend/.env
#    - packages/frontend/.env.local
#    - packages/meta-bot/.env
#
# See ENVIRONMENT_SETUP.md in the root directory for full configuration guide
# =============================================================================

# -----------------------------------------------------------------------------
# MongoDB Configuration (Docker Container)
# -----------------------------------------------------------------------------
# These are only used by the MongoDB Docker container
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=password123

# -----------------------------------------------------------------------------
# MongoDB Connection String
# -----------------------------------------------------------------------------
# This will be used by backend and meta-bot when running in Docker
# For local development, use: mongodb://localhost:27017/petbuddy
# For Docker Compose, use the service name: mongodb://admin:password123@mongodb:27017/petbuddy?authSource=admin
MONGODB_URI=mongodb://admin:password123@mongodb:27017/petbuddy?authSource=admin

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
#
# All other configuration should be in individual package .env files:
#
# 1. Backend Configuration:
#    File: packages/backend/.env
#    Template: packages/backend/.env.example
#    Docs: packages/backend/ENVIRONMENT_VARIABLES.md
#
# 2. Frontend Configuration:
#    File: packages/frontend/.env.local
#    Template: packages/frontend/.env.example
#
# 3. Meta-Bot Configuration:
#    File: packages/meta-bot/.env
#    Template: packages/meta-bot/.env.example
#
# =============================================================================
# QUICK START
# =============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update MongoDB password if needed
#
# 3. Configure individual packages:
#    cd ../packages/backend && cp .env.example .env
#    cd ../packages/frontend && cp .env.example .env.local
#    cd ../packages/meta-bot && cp .env.example .env
#
# 4. Update MONGODB_URI in each package .env to match this file
#
# See: ../ENVIRONMENT_SETUP.md for complete setup guide
# =============================================================================
